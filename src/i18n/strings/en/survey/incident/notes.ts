import type { LocaleMessage } from '@intlify/core-base'

const notes: LocaleMessage = {
  backupPowerSource:
    'A backup power source means a battery, auxiliary power unit (APU), ram air turbine (RAT), etc.',
  displaysDefinition: 'A “display” means EFIS, ECAM, EICAS, PFD, ND, or similar.',
  materialsAndLabor: 'Total repair cost includes estimated cost of materials and labor.'
}
export default notes
