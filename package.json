{
  "name": "root",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "build": "lerna run build",
    "serve": "lerna run serve --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
